# REQ-165: ビットストリーム内文字列型

## メタ情報

| 項目 | 値 |
|---|---|
| ステータス | draft |
| 優先度 | 低 |
| 依存 | なし |
| 作成日 | 2026-02-23 |
| 更新日 | 2026-02-23 |

## 背景・動機

現在のビットストリームモード（`mode: bitstream`）では、整数型（`uint8`, `uint16`, `uint32`, `uint64`）、`virtual`、`switch`、`struct` のみが許可されている。文字列型（`string`, `string0`）はビットストリーム内では使用不可。

一部のプロトコル（例: Bluetooth の広告パケット）ではビットフィールドと文字列フィールドが混在する。バイト境界にアラインされた文字列であれば、ビットストリームモード内でもデコード可能であるべき。

## 機能要件

### 追加する機能

- [ ] ビットストリームモードでの `string` 型サポート（バイト境界アライン必須）
- [ ] ビットストリームモードでの `string0`（NULL終端文字列）型サポート
- [ ] バイト境界にアラインされていない場合のバリデーションエラー

### 変更する既存機能

- [ ] `YamlToIrMapper` — ビットストリーム構造体のフィールド型チェック緩和
- [ ] `BinaryDecoder` — ビットストリーム内文字列フィールドのデコード処理追加
- [ ] `BitReader` — バイト境界アライン確認メソッド追加

### 変更しないもの（スコープ外）

- ビットアラインされていない文字列（ビット単位の文字列は非サポート）
- `bytes` 型のビットストリームサポート

## 受入条件

1. [ ] ビットストリーム内で `string` 型がデコードできること（バイト境界アライン時）
2. [ ] ビットストリーム内で `string0` 型がデコードできること（バイト境界アライン時）
3. [ ] バイト境界にアラインされていない位置で文字列型を使用した場合にエラーが発生すること
4. [ ] 文字列フィールドの前後にビットフィールドが正しくデコードされること
5. [ ] 既存テストが全て通過すること（`dotnet test` 全通過）

## 影響範囲

### 変更が必要なプロジェクト

| プロジェクト | 変更内容の概要 |
|---|---|
| BinAnalyzer.Core | 変更なし |
| BinAnalyzer.Dsl | `YamlToIrMapper` — ビットストリーム許可型リスト拡張 |
| BinAnalyzer.Engine | `BinaryDecoder` — ビットストリーム内文字列デコード、`BitReader` — アライン確認 |
| BinAnalyzer.Output | 変更なし |
| BinAnalyzer.Cli | 変更なし |

### 変更が必要なドキュメント

- [ ] docs/dsl-reference.md — ビットストリーム許可型リスト更新
- [ ] docs/architecture.md — 変更不要
- [ ] CLAUDE.md — 変更不要
- [ ] README.md — 変更不要

---

## 設計メモ

> 設計Phase（Phase 2）で記入する。要望定義時点では空欄でよい。

### 設計方針

### モデル変更

### インタフェース変更

### 代替案

### 懸念事項

---

## 実装メモ

> 実装Phase（Phase 3-4）で記入する。設計時点では空欄でよい。

### 実装中の設計変更

### 追加したテスト

| テストクラス | テスト名 | 対応する受入条件 |
|---|---|---|

### 気づき・今後の課題
